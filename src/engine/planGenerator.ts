/**
 * DEPRECATED — Replaced by aiPlanGenerator.ts
 *
 * This file contained the v1 rule-based plan generator and is no longer used.
 * Kept as an empty file to avoid import errors in case anything still references it.
 * Will be deleted in a future cleanup.
 */

// No exports — use aiPlanGenerator.ts instead
export {};
